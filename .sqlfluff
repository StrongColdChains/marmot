[sqlfluff]
dialect = ansi
templater = dbt
max_line_length = 80

[sqlfluff:templater:dbt]
project_dir = .
profiles_dir = ~/.dbt/
profile = marmot
target = dev

[sqlfluff:templater:jinja]
apply_dbt_builtins = True
